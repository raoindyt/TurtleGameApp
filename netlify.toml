# Netlify Configuration
[build]
  # Команда для сборки - пропускаем, т.к. папка уже собрана
  command = "echo 'Build already done'"
  
  # Папка с готовой сборкой для публикации
  publish = "public"

# Переадресация для SPA (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Настройки Node.js
[build.environment]
  NODE_VERSION = "22"

# Headers для безопасности (опционально)
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
